const sinon = require('sinon');
const { expect } = require('chai');

const MovieModel = require('../../models/movieModel');

describe("Recebe um ID para pesquisar", () => {
  describe("e o ID não é valido", () => {
    it('retorna false', async () => {

    })
  });

  describe('o ID é válido', () => {
    describe('não encontra o ID', () => {
      it('retorna nulo', () => {

      });
    })
  });

  describe('encontra o ID', () => {
    it('retorna um objeto', async ())
  })
})